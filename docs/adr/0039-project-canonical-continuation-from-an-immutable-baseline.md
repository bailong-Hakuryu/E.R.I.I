# Project canonical continuation from an immutable baseline

Canonical continuation starts from an immutable, versioned Relationship Baseline derived from qualitative premise meaning, cited Premise Experiences, and a deterministic Premise Policy; neither an LLM nor the host directly supplies numeric relationship state. Current state projects the baseline first and then folds live Relationship Events, preserving separate provenance and counts so imported backstory never masquerades as observed interaction and later history remains independently rebuildable.
